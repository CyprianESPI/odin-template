describe('example', () => {
    test('size test', () => {
        //If it should pass with deep equality, replace "toBe" with "toStrictEqual"
        expect([7, 8, 5, 8, 4, 3, 6, 1, 2].length).toStrictEqual([7, 8, 5, 8, 4, 3, 6, 1, 2].length);
    });
});